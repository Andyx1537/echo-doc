# QA 联合评审 · 搜索页「8-9 成目标规格」**复签报告** · 2026-08-04

| 项 | 值 |
|---|---|
| 评审类型 | **开发前置门禁复签**（`QA-CHARTER §0`）——首轮列必补项后，复核 PM 补齐版是否达标、签字锁基线 |
| 评审对象 | `SPEC-feature-pages.md` **附录 A · v2**（A.1~A.9，2026-08-04 PM 并入必补9+建议7 后版本） |
| 首轮报告 | `qa/QA-REVIEW-search-target-20260804.md`（结论：需补齐后再签 · 必补 9 · 建议 7） |
| 对照基准 | `SPEC §2.2` · `ACCEPTANCE TC-31` · `DECISIONS D4/D14/D18/D19` · `COPY-GUIDE` · `MOAT-GTM §7` · `DECISIONS G'' QA2`({items} 信封铁律) |
| 评审人 | 独立 QA（只读·只出评审意见·不改代码/规格） |
| **结论** | **✅ 同意签字（PASS）——附录 A v2 成为搜索页 L0 验收基线** |
| 统计 | **必补 9 项：9/9 落实 · 建议 7 项：7/7 落实 · A.8 新增验收点：全部到位 · 红线复核：全过** |

> 门槛（`QA-CHARTER §0.2`）：目标须「够完整、无歧义、可验收」。v2 已把首轮全部缺口写死到「开发照做即可」，无残留歧义，A.8 可逐条判定 → **准予签字**，即成 L0 基线。实现完成后 QA 按本基线逐条验收（L0），缺一条即退回。

---

## 1. 必补 9 项 · 逐条复核

| # | 首轮必补项 | v2 落实位置 | 判定 | 复核依据 |
|---|---|---|---|---|
| 必补-1 | 加载态 + 失败兜底可重试 + 竞态丢弃 | A.3（加载态/失败态/竞态三段） | **✅ 落实** | 「联想在途轻量占位、不转圈轰炸」「失败温柔兜底+可重试、不弹硬错误」「丢弃过期响应、只渲染最新 q」——三态齐、守安静基调、过词表 |
| 必补-2 | 每区最多 N 条 → 定死 | A.3「三个分区（各**最多 3 条**）」 | **✅ 落实** | N=3 已钉死 |
| 必补-3 | 「查看全部」去向（带 q） | A.3「查看全部去向」 | **✅ 落实** | 记忆→`?q=&type=windows`；用户→`?q=&type=users`；主题→复用广场 category；mock 亦返全量。去向明确 |
| 必补-4 | 三分区各 `{items,nextCursor}` 信封（对齐 QA2） | A.5 返回形状代码块 | **✅ 落实** | `windows/users/topics` 各套 `{items,nextCursor}`，消除首轮「{windows,users,topics} 遵 {items}」的矛盾，契约无歧义 |
| 必补-5 | 超长「安全截断」阈值 | A.5「最大输入长度 50 字」 | **✅ 落实** | 50 字钉死、输入端截断、超长不异常 |
| 必补-6 | 埋点 8 事件 + 属性维度 | A.6 | **✅ 落实** | 8 事件齐：`search_open{from}`/`search_query{q_len,has_result}`/`search_result_click{section,item_id,position}`/`search_history_clear{count}`/`search_hot_click{term}`/`search_view_all{section}`/`search_history_click{term}`/`search_history_delete_single`；并明写「不聚合公开榜」守 D19 |
| 必补-7 | 空区隐藏 + 双空兜底 | A.3 空区 + A.2 空态规则 | **✅ 落实** | 空区隐藏标题、三区全空进 A.4；双空（热门未配+无历史）给兜底屏「输入几个字，找找那段回忆」，对齐 D20 |
| 必补-8 | 去掉两处「或」二选一 | A.1 进入 + A.2 历史 | **✅ 落实** | 进入=「下往上滑入（定死此一种、不做整页切换）」；历史=「chip 平铺（定死 chip、不做列表）」 |
| 必补-9 | 返回/取消职责一致 + 输入框清空(×) | A.1 退出职责 + 输入框内清空 | **✅ 落实** | 「←返回 与 取消 行为完全一致=退出回广场、不丢滚动位置」；输入框右侧 × 清空回未输入态 |

**必补小结：9/9 全部落实、无歧义。**

---

## 2. 建议 7 项 · 逐条复核

| # | 首轮建议项 | v2 落实位置 | 判定 |
|---|---|---|---|
| 建议-1 | 清空历史二次确认 | A.2「确定清空搜索历史吗？+ 清空/再想想」 | **✅ 落实** |
| 建议-2 | 明确本版是否做词级联想 | A.3「本版不做词级联想下拉，直出分区结果」 | **✅ 落实**（明确 scope，消歧义） |
| 建议-3 | 高亮作用字段 | A.3「高亮字段=标题/昵称/近况/分类」 | **✅ 落实** |
| 建议-4 | 匹配规则（子串/大小写/拼音） | A.3「子串包含、大小写不敏感、本版不做拼音首字母」 | **✅ 落实** |
| 建议-5 | 运营热门词也过词表 | A.2 + A.7 均写入 | **✅ 落实**（红线闭环到运营配置） |
| 建议-6 | 结果卡字段白名单 | A.3「缩略图+标题+作者+暖光质性描述；不出现记得/献花/看过数」 | **✅ 落实**（红线落到字段级） |
| 建议-7 | 历史上限挤旧说明 | A.2 + A.3 提交逻辑均写「上限 10、挤出最旧」 | **✅ 落实** |

**建议小结：7/7 全部落实。**

---

## 3. A.8 完整度勾选（L0 验收基线）· 复核

首轮要求新增的验收点均已进入 A.8，且红线已拆条逐判：

- ✅ **加载态**（A.8：联想在途有轻量占位、不卡死）
- ✅ **失败态**（A.8：温柔兜底+可重试、过期响应被丢弃）
- ✅ **查看全部**（A.8：跳转带 q）
- ✅ **埋点逐事件**（A.8：8 个可触发可核、含关键属性、不聚合公开榜）——已从首轮笼统「埋点齐」细化为逐事件
- ✅ **红线拆条**（A.8 末条拆成 6 项独立勾选：无数字 / 无排名 / 不悲伤竞赛 / 历史仅本地 / 过词表(含热门词) / 结果卡不出现记得·献花·看过数）——符合红线零容忍、逐条判定

**判定：A.8 可作为 L0 验收基线逐条判定，无遗漏。**

---

## 4. 红线复核（逐条 · 零容忍）

| 红线 | 出处 | 判定 | 依据（v2） |
|---|---|---|---|
| 热门/结果无人数 | D4 · TC-31 | **过** | A.2/A.7 明写「无人数」；结果卡字段白名单「不出现记得/献花/看过数」（落到字段级） |
| 无排名 / 不悲伤竞赛 | D4/D14/D19 · MOAT §7 | **过** | A.2/A.7「无排名、不悲伤竞赛」；A.6 埋点「不聚合公开榜」守 D19 |
| 搜索历史仅本地、可清 | TC-31 | **过** | A.2「仅 localStorage、换设备不同步、单删/清空(带二次确认)」 |
| 文案过《温柔词表》（含运营热门词） | COPY-GUIDE · D6 | **过** | A.2/A.7 把「运营配置热门词也须过词表」写入，闭环 |
| 借完整度不借皮肤 | MOAT §7 | **过** | 附录 A 头「视觉沿用 Echo 现有基调、不新造风格」；借范式非皮肤 |

**红线全过；无任何签字阻断项。**

---

## 5. 复核观察（不阻断签字 · 实现期留意，非缺口）

以下为可选的实现期提示，**不影响本次 PASS**，无需改附录 A：

1. `search_history_delete_single` 未带属性——若后续数仓需分析「删了哪条」，可择期补 `{term}`；本版留空可接受。
2. 「主题」区「查看全部」复用广场 category 过滤，与记忆/用户的 `?q=&type=` 形态不同——已在 A.3 显式说明，属既定复用策略、非歧义。
3. 真接口 `GET /search?q=` 本轮标 TODO、先做 mock——已明确，符合「本轮 mock」范围；真接口落地时按 A.5 信封 + QA2 铁律验收。

---

## 6. 对 A.9「联合确认」栏的处置建议

```
### A.9 产品 + QA 联合确认
- 产品(PM)：已出目标 v2、并入 QA 首轮必补9+建议7 — ✅
- QA 联合评审：✅ 已复签（2026-08-04，依据 qa/QA-REVIEW-search-target-20260804-resign.md）
      —— 必补 9/9、建议 7/7 全落实，A.8 可逐条验收，红线全过；准予作为 L0 基线
- 制作人点头：☐ 待确认 → 通过即派技术实现
```

---

## 7. 最终结论

- **结论：✅ 同意签字（PASS）**。附录 A v2 已把首轮全部缺口写死到「开发照做即可、无需自行发挥」，无残留歧义；A.8 可作为 L0 验收基线逐条判定；红线逐条全过。
- **附录 A v2 自本复签起，成为搜索页开发的 L0 验收基线。** 请制作人点头后派技术实现；实现完成后 QA 按本基线（A.8）逐条验收，缺一条即退回。
- 本复签报告独立留档，不覆盖首轮 `QA-REVIEW-search-target-20260804.md`，保留审计链。

> 本评审只读、只出意见，未改任何代码或规格文档。A.9 的签字状态更新由 PM 编入附录 A。
