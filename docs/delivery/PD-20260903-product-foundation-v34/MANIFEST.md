# 级联交付账本 · PD-20260903-product-foundation-v34

状态：`DISTRIBUTED / AWAITING_REACK`
任务版本：`v4.2`
决策范围：`G-27`～`G-37`
产品基线提交：`34c9344`
v3 总账本与评论契约提交：`350c564`
v3 三方派发基线：`fda59c3`
v4 产品契约基线：`34c9344`
v4.1 字段契约修订：`8d506cf`
v4.2 最终契约修订：`be20048`
协调机制版本：以本文件所在提交为准
创建时间：2026-09-03

## 0a. v4 九项契约核实（2026-09-05）

| 项目 | 当前事实 | 是否已确认 | 下一责任方 |
|---|---|---|---|
| G-27～G-37 总账本升版与三方 ACK | v3 三方发现的产品待决项已按推荐定案并进入 `34c9344` | **本次升为 v4 并重新派发** | FE/BE/QA 按 v4 派发提交重新 ACK |
| Onboarding 最终状态机 | 主状态、失败恢复、生成前绑定、确认 CAS 与旧接口退出已冻结 | **已确认** | 三方复核可实现性、DTO 与用例 |
| Phase 0 首批事件/反馈/假设/动作字典 | v1 闭集与前后端采集归属已冻结，全部 `shadow=true` | **已确认** | 三方复核名称、字段和禁止项 |
| 行为原始/聚合/审计保存期限 | 180 天/24 个月/36 个月/3 年/5 年分层已写入规格和验收 | **已确认** | 后端落实删除作业，QA 以可控时钟验证 |
| 用户清除画像后的原始事件 | 立即停用并写 `usageBlockedAt`，不立即物理删除，原始事件按 180 天到期删除 | **已确认** | 后端阻断用途并执行删除，QA 验证不再训练/聚合/回填 |
| Work 审核凭证 | 完整字段、90 天最长复用窗、即时失效条件和防重放已冻结 | **已确认** | 后端落 schema/校验，前端展示过期/冲突，QA 验证 |
| 评论删除后的计数/分页 | 三加二、20/50 页长、keyset cursor、删除后补页、隐藏恢复和幂等冲突已冻结 | **已确认** | 三方按同一 API 样例复核 |
| 手机号短信供应商与频控 | `SmsProvider` 抽象和初始 TTL/频控已冻结；供应商由既有云账号和运维评估配置 | **产品契约已确认** | 采购/账号属外部依赖，不阻塞接口与风控实现 |
| 旧 Card 公开链 | 停新增、无公开兼容、无互动迁移；所属用户私域保留并适用既定数据生命周期 | **已确认** | 后端封口核存量，QA 验证 Plaza 仅 Work |

### v3 待决项关闭记录

上述九项已由 `G-37/FC1-FC8` 和 `34c9344` 关闭。若实施发现外部活跃旧客户端、短信采购不可用、法定删除要求或成本显著超出前提，必须触发停止闸重新评估，不得自行改契约。

## 0. 2026-09-03 后发裁定增量（G-34）

- 旧公开 MemoryCard 不迁移到 Work，不保留旧公开兼容链；原卡和媒体只作为所属用户的私域素材保留。
- 删除根评论时事务化级联软删整棵评论树，公开侧不保留占位；历史记录和审计继续保留。
- 清除适配档案采用软清除：撤销在线假设、决策、投影和缓存，行为事实与历史推断作为内部技术积累继续保留。
- 本增量已同步 `DECISIONS`、产品基线、专项规格、API 契约和验收；需前端、后端、QA 对新版本重新回执后才进入实现完成态。

## 1. 用户结果与主线定位

- 用户结果：匿名用户可理解产品、有限浏览公开作品、绑定后互动；完成单宠私域建档并收到内容；用户可将内容形成作品公开；系统通过受控行为证据改善体验。
- 主线：安顿 → 私域相处 → 形成作品 → Plaza 被看见 → 互动消息回流。
- 系统：账号、建档、私域窗口、生成、作品、审核、Plaza、互动、消息、行为适配、QA。
- 非目标：本账本不直接授权开发；不处理旧客户端兼容实现；不让公共行为自动进入私域生成。

## 2. 四层差额

| 层 | 结论 |
|---|---|
| 以前确认的产品方案 | 公开层主要围绕回忆卡，所有未绑定写操作均拦截，旧建档为通用选择链，推荐隐式信号完全禁读 |
| 当前实际实现 | 作品已有创建/列表/详情/软删骨架；Plaza、互动、审核、消息仍混有 cardId 旧链；匿名账号和通用绑定骨架存在；单宠会话与行为四账本不存在 |
| 本次确认的新方案 | Work 是唯一公开实体；匿名有限浏览与生成前绑定；一窗一宠沉浸问卷；行为事实、反馈、假设、决策分存且隐式信号仅影子 |
| 精确开发差额 | 必须先补统一 Work 契约、手机号生产契约、匿名批次、建档会话契约、事件字典与 G-27～G-30 验收总表，再冻结三方契约后拆开发 |

被替代重点：公开回忆卡主线、匿名上传一律禁止、旧通用建档、隐式行为连采集也禁止的宽泛解释。

保留不变量：回忆卡私域；公开互动只归 `workId`；绑定不更换 `accountId`；服务端拥有业务权威；陌生人互动不进入私域生成；禁止敏感推断。

## 3. 首轮三方回执（v1 历史记录）

### 前端

- ACK：`accepted-with-risks`；可做路由、状态矩阵和只读骨架准备，不可宣称整链就绪。
- 可复用：Work DTO/适配器、发布三步、作品瀑布骨架、匿名 token、通用错误与 AI 标识。
- 新增：真实作品详情、互动评论、全局手机号登录与原路续接、匿名批次末态、作者审核/申诉前台、契约和真联调测试。
- 迁移：Plaza 从 Card/Window 转 Work；公开详情、互动、消息全用 `workId`；`GET /works` 从公共流收口为作品墙语义。
- 阻塞：作品互动/评论、手机号验证码、作品审核申诉接口未冻结；前端仓存在未确认的未提交修改，写任务派发前必须先定归属和安全分支。
- 预计：契约冻结后双前端并行约 2～4 周；单人约 3～5 周，置信度中，不含等待时间。

### 后端

- ACK：`accepted-with-blockers`；G-30 Phase 0 可进入字段级契约设计，跨端实现仍需联合冻结。
- 可复用：匿名账号/token/绑定骨架、Work 表与基础端点、卡审核基础设施、素材归属和私域生成基础。
- 新增：公开审核凭证与冻结快照、作品审核和互动事实、匿名批次与手机号挑战、建档会话/事实分存、行为四账本和作用域权限。
- 迁移：公开读写从 cardId 迁至 workId；公开卡停止新增；卡审核仅复用设施；散落日志迁为可审计行为事实。
- 阻塞：匿名上传与旧绑定规则冲突、Work 全链契约未冻结、审核凭证字段、手机号供应与冲突策略、建档阈值、首批行为字典。
- 预计：按“依赖门 + M/L/XL 工作量 + 置信度”评估；依赖冻结后重新给有效开发日窗口。

### QA

- QA_ACK：已接收并完成初评。
- Readiness：`REWORK`；产品方向认可，但当前不可进入跨端编码。
- 当前允许：技术盘点、接口/schema/状态机方案、差异和工期评估。
- 当前禁止：跨端实现、合入或宣称 G-27～G-30 完成。
- 核心原因：`ACCEPTANCE.md` 未同步新四组裁定；多项接口仍为建议/二选一；匿名批次、建档、行为字典没有可执行闭集。
- 验证门：文档复签 → 契约/迁移 → 分域功能 → 真联调 → 全量红线和候选签收。

## 4. 联合冻结门（v1 历史记录）

- FE_ACK：`accepted-with-risks`
- BE_ACK：`accepted-with-blockers`
- QA_ACK：`rework`
- 契约版本：未冻结
- 开工结论：`NOT_READY`

由于三方未共同接受同一契约，不分配实现 agent。协调方下一步只分发以下产品/契约补齐任务：

1. `G-27`：锁定 Plaza/Works/WorkDetail、作品互动、评论、审核/申诉、消息的唯一端点和 DTO；
2. `G-28`：锁定手机号验证码、绑定冲突、匿名批次边界和评论预览契约；
3. `G-29`：把建议接口转成请求、响应、错误、幂等、恢复和并发契约；
4. `G-30`：冻结 Phase 0 首批事件、动作和假设维度字典；
5. QA：将四组新场景并入 `ACCEPTANCE.md`，删除或标记被替代的旧判据。

## 5. 停止项与恢复条件

当前停止原因：公开对象、匿名写权限、登录、审核、评论和建档接口仍有根基/契约差异。继续让前后端分别编码会重现“各自完成、真联调断裂”。

恢复条件：上述五项补齐 → 产品与技术联合定调 → QA 完成可测性复签 → 基线/契约升版并推送 → 本账本发布 `v2` 差额 → FE/BE/QA 重新 ACK → 才能进入 `READY_FOR_DEVELOPMENT` 并按依赖拆 agent。

## 6. 首轮验证结论

本轮已验证级联机制能够在开发前发现：

- 前端已有页面骨架不等于作品详情链完成；
- 后端已有表和路由不等于作品审核/互动状态机完成；
- Mock 与单端绿色不能证明真链路；
- 旧验收基线若不先更新，会拿已失效规则判新实现；
- 一个聚合任务包过大，应在契约补齐后按 G-27～G-30 拆成可独立验收的功能切片。

历史最终状态：`STOP_PRODUCT_REVIEW`，未授权实现。该结论已由 v3 重新派发取代，不代表当前账本最终状态。

## 7. v3 重新派发与开工边界

- FE_ACK：`accepted-with-risks`
- BE_ACK：`needs-product-decision`
- QA_ACK：`rework`
- 当前允许：状态机/schema/API 字典、迁移盘点、固定契约样例、页面状态骨架、QA 测试数据与用例设计。
- 评论前端已确认：默认三加二、剩余 N 条、匿名登录原路续接、热门/最新且前端不重排；可据此制作页面状态和契约样例。
- 当前禁止：把建议接口当冻结契约、仅凭 Mock 宣称完成、在保存期限/审核有效期/短信采购未定时写死不可迁移常量。
- 局部可开工：旧 Card 公开读写封口、行为四账本可迁移骨架、Work 审核凭证字段骨架、Onboarding 持久会话骨架、评论软删与角色权限骨架。
- 进入 `READY_FOR_DEVELOPMENT`：三方回执齐全，技术方案已补齐可执行契约，QA 复签，需产品确认的四项已有明确结论或被拆为不阻塞当前骨架的后续里程碑。

## 8. v3 三方回执摘要（2026-09-05）

### 前端

- 可立即做：页面状态、路由恢复、DTO/Mock 夹具、Work 详情与作品墙骨架、评论三加二、手机号状态组件、Onboarding 页面组件、行为上报 SDK 外壳。
- 阻塞真接线：Plaza/Work DTO、Onboarding 状态机、行为字典闭集、评论游标/错误/隐藏恢复、审核冲突展示。
- 工程风险：客户端 `develop` 有 9 个未提交文件，且现有 Plaza 改动仍朝 `CardView/PlazaCard`，与 Work 唯一公开对象相反；写任务前必须确认归属并停止或迁移该方向。

### 后端

- 可立即做：Onboarding 持久会话六表、行为四账本与版本字典、Work 内容版本与审核凭证表、Work 评论/软删/权限/幂等、短信适配抽象、旧 Card 公开写封口和调用监控。
- 已提出但未定案：Onboarding 状态机、Phase 0 v1 字典、分层保存期限、审核凭证 30/90 天、评论游标与错误码、短信供应商和频控、审核并发组合约束。
- 实现事实：当前匿名绑定不失效旧 token；Onboarding 仍为进程内 Map；Plaza 仍读公开 Card；Work 尚无版本/审核凭证/评论；短信能力不存在。

### QA

- 可先做：测试矩阵、固定数据、契约样例、评论页面/DTO/软删骨架验证、旧 Card 封口验证准备。
- `REWORK` 原因：Onboarding 旧验收仍要求“上传前绑定”，与 G-29“生成前绑定”冲突；最终状态机、行为闭集、保存期、审核凭证时窗、短信生产参数未冻结。
- 真联调最低环境：真实前后端与数据库、可控时钟、服务重启/多实例、并发注入、短信沙箱、审核策略版本切换、审计只读探针。

## 9. v3 专业提案与采纳结果

1. **Onboarding**：内部状态建议 `collecting → ready_to_bind → ready_to_generate → generating → candidate_ready ↔ refining → ready_to_confirm → confirmed`；失败记录为可恢复的 `lastOperation`，不滥增主状态。若无外部已发布客户端，新客户端切换后直接封旧接口；若存在活跃客户端，建议兼容 30 天、最长 60 天。
2. **Phase 0 v1 字典**：后端已提交 Onboarding、生成反馈、Plaza 事件，以及 `likeness/ease/continue_intent/change_request/less_like_this` 反馈、三域弱假设和九项白名单动作；全部影子运行，禁止敏感推断与跨域。
3. **保存期限建议**：原始事件 180 天；用户级聚合/历史假设与决策 24 个月；不可逆去标识群体统计 36 个月；身份/授权/画像清除审计 3 年；公开内容审核/安全处置审计 5 年。
4. **画像清除**：已定立即停止使用；后端建议写 `usageBlockedAt`，原始事件不再训练/聚合/回填，并按 180 天普通期限物理删除。
5. **Work 审核凭证**：后端补充 `reviewEvidenceId/sourceCardId/sourceContentVersion/ownerAccountId/expiresAt/invalidatedAt/invalidationReason/consumedByWorkId` 等字段；有效期建议 90 天，严格方案 30 天；策略重大升级立即失效。
6. **评论**：默认根分页建议 20、最大 50，回复同；不透明 keyset cursor；稳定错误码与幂等冲突规则由服务端负责。作者隐藏建议可恢复，仅作者治理视图可见。
7. **短信**：建议 `SmsProvider` 抽象；已有云账号优先阿里云，腾讯云可作候选/灾备。初始建议：验证码 5 分钟、重发 60 秒、单挑战错 5 次、号码 5 次/小时与 10 次/日、设备 10/小时与 30/日、IP 20/小时与 100/日、解析凭证 10 分钟单次。
8. **审核并发 H19**：技术候选为提交按 `(workId, contentVersion)` 去重，处置按 `(moderationId, expectedStateVersion)` CAS；是否增加 work 级活动工单唯一约束待产品确认。

上述提案已于 2026-09-05 按推荐执行并写入 `G-37`、专项规格、API 与验收。供应商名称仍为运维配置；除此之外不得再把本节称为“待产品确认”。

## 10. v4 统一差额与重新派发（2026-09-05）

- 基线：`34c9344`；范围扩展为承接后发决定的 `G-27～G-37`。
- 主线不变：Card 私域、Work 公开、Plaza 只分发 Work、公开互动归 `workId`、陌生人行为不进入私域生成。
- 新增冻结：Onboarding 状态机和旧接口退出、Phase 0 v1 字典、数据生命周期、画像清除、90 天审核凭证、评论 20/50 与隐藏恢复、短信初始风控、审核组合并发。
- 被替代：S1′ 与旧验收中的“上传前绑定”收窄为“匿名可私域试填，生成前绑定”。
- 前端复核：脏文件归属、Plaza 迁 Work、登录原路恢复、服务端能力/分页/计数/冲突只展示不自算。
- 后端复核：设备凭据拆分、持久 Onboarding、行为字典和删除作业、Work 审核/评论/提交通道、短信适配、旧 Card 封口。
- QA 复核：v0.6 验收、真实边界环境、可控时钟、重启/多实例、并发注入、短信沙箱、策略纪元和审计探针。
- 复签门：FE/BE/QA 对同一 v4 提交返回 ACK；任一 `rework/blocked/needs-product-decision` 继续停在契约评审，不派实现 agent。

## 11. v4 首轮复签与 v4.1 修订（2026-09-05）

- FE_ACK：`accepted-with-risks`；产品方向可实施，要求补完整 API DTO、隐藏/恢复端点、短信响应和逐事件 emitter。
- BE_ACK：`accepted-with-risks`；无新增产品待决项，产品语义足够开工；要求字段级契约和运行方案由后端实现提交补齐。
- QA_ACK：`rework / needs-product-decision`；确认旧“上传前绑定”冲突已消除，但发现状态枚举、隐藏占位、错误响应和作业契约仍不一致。
- 产品澄清：隐藏评论从所有普通列表过滤且不占位；只有旧直达、已打开页面或继续操作时显示通用不可用；不是保留列表占位。
- v4.1 修订 `8d506cf` 已补：Onboarding 完整状态/DTO/CAS/错误；评论 hide/restore 与游标错误；Phase 0 字段级闭集；生命周期边界与作业审计；审核凭证边界/原因码；短信 E.164、滑动窗口、旧码失效和稳定错误；活动工单状态与事务回滚。
- 当前重新派发基线：以本账本所在提交 + `8d506cf` 为准。三方只复核新增差额；不得重开已关闭的产品方向。

## 12. v4.1 二次复签与 v4.2 修订（2026-09-05）

- FE_ACK：`accepted-with-risks`；指出多选答案、已有手机号切号续接及事件 emitter 通配风险。
- BE_ACK：`blocked`，且仅限 Onboarding 多选、可选短输入和授权版本 DTO；其余模块已确认可开发。
- QA_ACK：`blocked`；仅剩多选 DTO、全局错误信封和评论隐藏验收措辞三项。
- v4.2 修订 `be20048`：答案改为 `answerCodes[]` 并支持可选短输入/语音转写来源；快照和确认增加授权版本；`switch_existing` 私域建档固定不续接；所有新增错误统一 `{code,msg,detail,data}`；评论验收限定“列表无占位、仅旧直达/已打开/继续操作显示不可用”；Phase 0 emitter 改为逐事件明确归属并补字段类型与必填性。
- 当前重新派发基线：以本账本所在提交 + `be20048` 为准。本轮目标是最终 `READY_FOR_DEVELOPMENT` 复签。
