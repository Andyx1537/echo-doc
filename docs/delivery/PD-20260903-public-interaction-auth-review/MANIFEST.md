# 级联交付账本：公开互动、账号切换与审核重提

任务 ID：`PD-20260903-public-interaction-auth-review`

决策范围：`G-31`–`G-36`

状态：`PRODUCT_CONFIRMED / TECH_ASSESSING`
基线分支：三仓 `develop`；基线标签 `baseline-2026.09.03`
本轮产品与契约基线：`echo-doc/develop@f91a200`

## 统一增量

| 项目 | 内容 |
|---|---|
| 原有基线 | Work 是唯一公开实体；匿名可读详情但公开写需手机号；驳回/审核存在目标状态但互动、手机生产契约不完整 |
| 当前实现事实 | 前端缺完整 WorkDetail、评论互动和真实手机号流程；后端有 Work/匿名账号骨架，但缺作品评论收藏、手机号挑战解析和作品版本化重提的完整链路 |
| 新定案 | 评论两层并保留实际回复对象；普通用户只删自己内容，作者可隐藏/删除/举报但不编辑他人，平台全局治理；游客 Plaza 详情显示热门一级前三条及受控回复预览；设备凭据只登录未绑手机匿名账号，绑定后失效且再次使用创建新匿名账号；用户级投稿单通道按 `uploading/submitting/pending` 占用；驳回同 workId 重提，服务端计算权威 hash |
| 不变项 | 公开事实只归 workId；匿名写零落库；手机号账号必须手机号登录；前端不拥有身份或名额判断权；私域资料不因陌生人互动回流；下架申诉不能被编辑入口绕过 |
| 失败策略 | 权限、关系、验证码、版本或审核条件不明均 fail closed；手机流程失败保留匿名会话；旧 token 不可重放 |

## 拆分工作线

1. `WI-COMMENT`：评论 DTO、二级封顶关系、热门/最新读取、删除治理、真接口与页面状态。
2. `WI-FAVORITE`：私有收藏、本人收藏页、作者/公开信息零泄露。
3. `WI-PHONE`：挑战、验证、归属解析、切换确认、会话替换与返回路径。
4. `WI-RESUBMIT`：作品内容版本、编辑草稿、重提 CAS、审核工单和作者前台。
5. `WI-QA-INTEGRATION`：契约样本、迁移、真后端联调、受影响回归与精确构建签收。
6. `WI-DEVICE-AUTH`：设备凭据只恢复未绑定匿名账号、绑定时原子失效、再次使用创建新匿名账号和前端不透明存储。
7. `WI-SUBMISSION-SLOT`：用户级唯一投稿名额、能力字段、原子准入、撤回/网络失败释放和作品墙状态呈现。

## 三方回执门

- 前端：页面/路由/草稿与账号切换状态、最小接口缺口、分支和预计窗口。
- 后端：schema/API/事务/审计/迁移/安全与预计窗口。
- QA：可测性、数据与环境、用例映射、阻断项和签收条件。
- 三方共同确认同一 API/DTO/错误/状态版本前，状态不得进入 `CONTRACT_READY`。

## 产品仍需决定

产品已确认设备凭据边界、评论角色权限、游客预览场景和唯一投稿名额状态。技术下一轮必须提出：评论预览/分页与错误标识、隐藏恢复、评论幂等与并发；设备凭据轮换和并发新建去重；投稿请求已受理但响应丢失时的恢复；审核并发锁作用域 `H19`。产品只确认用户展示与恢复，不替技术指定锁或分页实现。热门评论权重、作者删评二次确认、收藏页分组仍属非阻塞体验配置。

## 2026-09-04 派发增量

- 前端：设备信息仅作不透明存储；评论角色菜单和游客预览只读服务端能力；投稿状态只在“我的作品墙”与详情展示，不新增审核进度中心。
- 后端：绑定手机号原子撤销设备登录关系；旧设备标识不登录绑定账号；评论角色权限和根树删除由服务端执行；用户级投稿名额原子判断并下发阻塞作品。
- QA：验证设备凭据不能进入已绑定账号、作者无编辑他人入口、根树删除与二级删除差异、游客预览不可绕过、并发投稿最多一条链及网络失败/撤回释放。
- 待技术回执：分页、错误码、幂等、并发保护、隐藏恢复及 `H19` 审核锁作用域；三方 ACK 前保持 `TECH_ASSESSING`。
