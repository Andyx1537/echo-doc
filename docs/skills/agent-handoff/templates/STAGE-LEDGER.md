# 阶段账本 · <task-id>

状态：`OPEN` | `BLOCKED` | `CLOSED`
依据：`CURRENT-DELIVERY-STATUS` 编号 / 交付账本 deliveryId
更新：YYYY-MM-DD
执行约定：`docs/skills/agent-handoff/SKILL.md`

## 概述

- 这一份要交付的结果（一句话）：
- 不做（非目标）：
- 权威来源：
- 接手先读：本页未勾选的下一块；不要通读对话。

## 占用

| 资源 | 谁 | 路径/分支/工作树 | 释放条件 |
|---|---|---|---|
| 无 | | | |

有未提交文件时必须列出完整路径。占用方离开前改「无」或写明继任。

## 阶段

| 块 | 状态 | 提交 | 验证 | 备注 |
|---|---|---|---|---|
| 1. | `todo` | | | |
| 2. | `todo` | | | |

状态只用：`todo` / `doing` / `done` / `blocked`。`done` 必须有提交号。

## 下一块入口

接手方从这里开始：

```text
仓库：
分支：
命令或文件：
禁止改动：
```

## 发现（当初不知道的）

只记新事实，不复述已写入文档的内容。
