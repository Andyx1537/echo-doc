# 派线清单 · 2026-08-30

> 用途：制作人重启 Cursor 后，**按专业分线派 agent** 时照着这份派。  
> 每条线的段落**可整段转发**，自带真源、边界、验收与交付物，不需要再补上下文。

## 概述

- **四条线，互不重叠，可并行也可串行。** 文件归属已按线划死（§0.2），
  本项目出过「一个代理的成果被另一个代理覆盖」的事故，所以**不许跨线改文件**。
- 🔴 **L4（合规改软删）是唯一带破坏性 schema 变更的线，且需要制作人先拍一个板**
  （本期做 / 记进风险台账），没拍板前不要派。
- 📋 **L1、L2、L3 都不是从零写**：三份目标文档都已存在且有实质内容
  （`SPEC-security` 488 行、`TECH-AI-PLATFORM` 213 行、前端组件齐全）。
  派线时务必写明「补，不要重写」——否则会得到一份把现有内容洗掉的新文档。
- 🔴 **主线另有一个更靠前的断点，四条线都不解决它**：
  「发布卡 → 进共鸣厅」在服务端**没有任何创建卡的入口**
  （见 `PRODUCT-IMPLEMENTATION-AUDIT.md §0`）。四条线做完，主线仍然是断的。
  什么时候动它，需要制作人单独排。

### 0.1 四条线一句话

| 线 | 做什么 | 前置 | 破坏性 |
|---|---|---|---|
| **L1** 安全审计出入口 | 把 65 条真实路由逐条挂到 `SPEC-security §4` 的 12 个场景下，挑出无归属的 | 无 | 无（只改文档） |
| **L2** AI 中台架构 | 补 `TECH-AI-PLATFORM.md`，重点是 §10 那批开放问题与「情感记忆」这一层 | 无 | 无（只改文档） |
| **L3** 窗下翻 + 回声换一批 | 前端两个交互，含空态与到底了 | 需先定接口是否够用 | 无（前端） |
| **L4** 三处物理删改软删 | 🔴 红线修复，加软删列 + 改去重 + 补 partial 索引 | **等制作人拍板** | 🔴 schema 变更 |

### 0.2 文件归属（🔴 不许跨线改）

| 线 | 只许改这些 |
|---|---|
| L1 | `echo-doc/docs/SPEC-security.md`、`echo-doc/docs/security/**` |
| L2 | `echo-doc/docs/TECH-AI-PLATFORM.md` |
| L3 | `echo-client/echo-h5-proto/src/**` |
| L4 | `echo/echo-server/src/main/**`、`echo/echo-server/src/main/resources/sql/schema.sql` |

公共文件（`DECISIONS.md`、`WORKLOG.md`、`PRODUCT-MINDMAP.md`）**任何线都不许直接改**，
改动建议写进各自交付物的「待并入清单」，由制作人转交。

### 0.3 派线纪律（🔴 上一轮的教训，必须带进 prompt）

上一轮 489 个子代理里 **125 个是 aborted**（26%），中止的多半是上下文耗尽。
所以每条线的 prompt 里必须写死这四条：

1. **一节一提交，不许攒。** 提交是唯一不会随进程蒸发的东西。
2. **要改某条结论就 grep 到那一条，不要通读整份文档。**「为了确认没漏而通读」正是爆上下文的动作。
3. **回执只写三样**：落了哪些编号、改了哪些引用方、**发现了什么交办时不知道的事**。
   不要复述已经写进文档的内容。
4. **落盘前回代码核实状态。** 文档里的「已实现／未实现」记的是它写下那一刻的事实，不是现在的。

---

## L1 · 安全审计现有出入口

**目标**：让「这个端点的安全要求在哪一节」这个问题有确定答案。现在没有。

> 🔴 **2026-08-30 回执后留痕：本节「68 个出入口」的前提被 L1 的结果推翻。**
> 全量是 **83 条注册入口 / 生产可达 82 条**（`SPEC-security §4.13`）。差额里最要命的是
> **WebSocket 8 条**——它不是 HTTP，**数路由表数不出来**，本节下面那句
> 「按路由表做的审查一个都扫不到」说对了病，但只说到了浅的那一层。
> 排期请引用 `SPEC-security §4.13/§4.14`，**不要引用本节的数字**。以下保留原文备查。

**现状（2026-08-30 实测）**：

- Router 里有 **65 条路由**（`EchoApi` / `ModerationApi` / `GovernanceApi` / `LeaveWordsApi`）。
- 🔴 **另有 3 个出入口不在 Router 里**，共 **68 个**。它们由 `HttpGateway` 直接
  `createContext` 或在 dispatch 前拦截，**按路由表做的审查一个都扫不到**——
  而这 3 个恰好是风险最高的：

  | 出入口 | 鉴权 | 说明 |
  |---|---|---|
  | `POST /api/v1/upload` | 有（登录 + 绑定） | 唯一上传口，25MB 上限。`HttpGateway:146 handleUpload`，**不在 Router**（要原始字节 + multipart）。🔴 代码注释自己写着「不会被任何按路由表做的审查扫到」 |
  | `GET /api/v1/files/{key}` | 🔴 **无** | 用户上传的照片/音视频**公开按 key 下发**，`HttpGateway:189 serveFile`。靠 resourceId 不可猜（随机 63 位）挡人，**没有鉴权、没有吊销**——卡被下架或删除后这个 URL 照样能开 |
  | `GET /healthz` | 无 | 探活 |

- `SPEC-security §4「逐页面 / 逐出入口」`分 **12 个小节**（4.1 建档 … 4.12 分享落地页），
  🔴 **按页面场景组织，不按端点**——整章只点名了 4 条 HTTP 路径
  （`DELETE /pet/:id`、`DELETE /pet/me`、`GET /w/{windowId}`、`POST /share/og-image`）。
  §4.10 有 `/files/{key}` 一节，但**没有登记 `/upload`**。
- **所以覆盖率现在无法判定**：不是"没覆盖"，是**映射不存在，判不了**。

🔴 **给本线的方法论警告（这条比数字重要）**：
**不要只从 `r.add` 枚举出入口。** 我第一次就是这么数的，得到 65，漏掉了唯一的上传口
和唯一的公开文件下发口——**而安全审计最该看的就是这两个**。
枚举必须同时覆盖 `HttpGateway` 的 `createContext` 与 dispatch 前的路径拦截。

**要产出的**：

1. 一张 **68 行的映射表**（端点 → §4 哪个小节 → 该节对它提了哪几条要求）。
   放进 `SPEC-security` 新开一节，编号接在 §4 之后。
2. 🔴 **挑出「挂不上任何小节」的端点**，逐条说清为什么挂不上：
   是场景漏了（要补小节），还是端点本身不该存在（要下线）。
3. 对高风险面单独点名。至少这几类要有说法：
   - 🔴 `GET /api/v1/files/{key}` **无鉴权、无吊销**：卡下架/删除后 URL 仍可开。
     这与 `CM-G4`（签名 URL + 水印 + 缩略图派生，原图最小暴露）是正面冲突，
     §4.10 现有内容需要按这个事实重写
   - 🔴 `POST /api/v1/upload` **§4 完全没登记**，而 `S1′` 点名的「上传素材」就在这儿
   - `/admin/**` 六条（`AdminRoles` 怎么判？§4 没有后台这一节）
   - `/auth/guest` 是 Router 里唯一的 `addPublic`，它能拿到什么
   - `/plaza`、`/users/:id/cards`、`/windows/:petId` 三条陌生人可达的读接口的下发面

**边界**：🔴 **只改 `SPEC-security.md` 与 `docs/security/`。不改代码，不改其他规格。**
需要改别处的写进「待并入清单」。

**验收**：68 个一个不落地出现在映射表里；挂不上的那些有单独清单和理由。

**怎么取那 68 个**（可复核，🔴 两条都要跑，只跑第一条会漏掉上传与文件下发）：

```bash
# ① Router 里的 65 条
cd echo/echo-server/src/main/java/com/echo/http
rg -o 'r\.add(Public)?\("(GET|POST|PUT|PATCH|DELETE)", "[^"]+"' *.java

# ② Router 之外的 3 个
rg -n 'createContext|equals\("/upload"\)' HttpGateway.java
```

---

## L2 · 情感记忆 AI 中台架构文档

**目标**：把 `TECH-AI-PLATFORM.md` 从「架构与路线」补到「可据以动工」。

**现状**：文档已存在，213 行，骨架完整（0 核心原则 / 1 现状盘点 / 2 分层与边界 /
3 可复用能力 / 4 build-vs-buy / 5 本地养模型路线 / 6 Echo 怎么消费 / 7 多 app /
8 红线合规 / 9 代价 / 10 开放问题）。

🔴 **不要重写这份文档。** 它的判断（尤其 §4 分场景的 build-vs-buy 与 §9 代价）是有价值的。

**要产出的**：

1. **§10 的开放问题逐条收口**，或者标明「定它需要什么前置」。
   现在这一节是敞着的，敞着就没法排期。
2. 🔴 **补「情感记忆」这一层本身的定义**——文档名字里有它，但架构里它是什么
   （一张表？一个服务？一组 embedding？）需要写死。这是本次要求的重点。
3. **§1 现状盘点回代码复核一遍**。它标称「基于真实代码」，但代码在 8-28 拆过仓，
   包路径与仓位置都变了（现在是 `echo/echo-server/src/main/java/com/echo/`）。
4. 与 `AI-CAPABILITIES.md` 的分工写一行指针，别两份文档各写一遍同一件事。

**边界**：🔴 只改 `TECH-AI-PLATFORM.md`。§8 红线那节涉及合规的，
以 `SPEC-trust-and-compliance` 为准，本线不改那份。

**验收**：§10 每条要么有结论，要么有「定它需要什么」；「情感记忆」在架构图上能指出来。

---

## L3 · 窗下翻 + 回声换一批

**目标**：两个前端交互。

**要产出的**：

1. **窗下翻**：窗口页往下翻的分页/加载，含**空态**、**加载态**、**到底了**三个中间态。
2. **回声换一批**：换一批的触发、节流、以及「换不出新的了」怎么说。

🔴 **本线受 `visual-decisions` 规则约束**：这两件事都落在用户看到的那一屏上，
**必须出渲染图对比，不能用文字描述**。要求见 `.cursor/rules/visual-decisions.mdc`，
重点是：同一批数据、390 手机视口、造像真的数据（占位符不算）、
比较"某块出不出"时其他本来不可见的块也要一并裁掉。

**前置**：先确认服务端接口够不够用——现有的是 `GET /pet/me/echoes` 与
`GET /windows/:petId`，**是否支持分页与"换一批"语义需要先查**，不够就先出接口需求，
不要在前端硬造。

**边界**：🔴 只改 `echo-client/echo-h5-proto/src/**`。要改接口的写需求，不要自己动后端。

**验收**：三个中间态各有一张 390 视口截图；换一批的节流规则写进组件注释。

---

## L4 · 🔴 三处物理删除改软删（等拍板后再派）

**这条线在制作人拍板前不要派。** 需要拍的是：本期做，还是记进风险台账下期做。

**为什么是红线**：`SPEC-trust-and-compliance §CM-G0-1` 正文点名举的例子就是「取消记得」，
要求开关型取消**不得物理删行**，改为 `active=false` + 时间戳。而代码实际：

| 动作 | 实现 | 位置 |
|---|---|---|
| 取消记得 | `DELETE FROM "t_remember"` | `PgEchoStore.java:512` |
| 取消关注 | `DELETE FROM "t_follow"` | `FollowStore.java:141` |
| 取消拉黑 | `DELETE FROM "t_block"` | `BlockStore.java:142` |

**代价（拍板前要知道的三件事）**，详见 `AUDIT-compliance-implementation-gap.md §5`：

1. **去重语义要同一次迁移一起改。** 现在靠 `ON CONFLICT ("petId","accountId") DO NOTHING`
   防重复；改软删后同一对会有多行（记得→取消→再记得），
   唯一索引必须同时改成 partial，分两次做中间态一定出错。
2. **读路径要全部带过滤**，漏一处就是"取消了还生效"。`hiddenBetween` 这类被广场与窗共用的判断尤其要盯。
3. **存量取消动作已经物理删掉了，找不回来。** 迁移只能让新数据留底，
   这一点要写进风险台账，别让协议承诺覆盖到存量。

🔴 **顺序**：先加软删列并改写取消逻辑，**再**做 `§CM-G0S S-7`（部分唯一索引）。
反过来做会得到一个谓词恒真的索引——不报错、不生效，又一个静默假动作。

---

## 附：本轮已完成、不用再派的

| 事 | 结论 | 提交 |
|---|---|---|
| 合规 A/C/D 三组「补齐」 | **不用补，文档层面已全齐**；缺口在实现侧 | `766f8e9` |
| 拆仓四笔尾巴 | 全清 | `38da7a4` `9d85785` `5858a44` `bc9426a` · `echo 7dd6bd6` |
| 发布卡实现状态 | 服务端零入口，主线第 10 步断开 | `e78df12` |
